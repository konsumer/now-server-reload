{
  "name": "@demo/now-server-reload",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "deploy": "now",
    "start": "now dev",
    "lint": "standard --parser babel-eslint --fix"
  },
  "devDependencies": {
    "babel-eslint": "^10.0.1",
    "now": "^15.2.0",
    "standard": "^12.0.1"
  }
}
